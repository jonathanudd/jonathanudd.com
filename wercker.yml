box: ubuntu
build:
  steps:
    - arjen/hugo-build:
        version: "0.14"
deploy:
  steps:
    -install-packages:
        packages: git
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        domain: jonathanudd.com
        basedir: public
